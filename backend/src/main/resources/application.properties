# MySQL Database Connection
# Update this line with your actual AWS RDS Endpoint and database name
spring.datasource.url=jdbc:mysql://trainitup-db.ce5kq2uu0zgr.us-east-1.rds.amazonaws.com:3306/railway
spring.datasource.username=root
spring.datasource.password=PBhDCHhQw9YMwbK

# Tell Hibernate to automatically update tables if we change our Java code
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Set the server port to 8080 (or Render's PORT if provided)
server.port=${PORT:8080}

# JWT Configuration
jwt.secret=trainitup_secret_key_that_should_be_at_least_32_characters_long_for_security
jwt.expiration=86400000

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

logging.level.org.springframework.security=DEBUG

# File Upload Configuration
file.upload.dir=uploads/courses
file.upload.max-size=10485760

# Multipart file upload configuration
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# CORS allowed origins for dev
cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5500,http://127.0.0.1:5500

# Actuator health endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true